# OS/editor noise
.DS_Store
Thumbs.db
desktop.ini

# Python/Node noise (future-proof)
__pycache__/
.pytest_cache/
node_modules/

# Local env and secrets
.env
.env.*
secrets/
credentials/

# Build/output artifacts (reserved for future implementation phase)
out/
dist/
tmp/
tests/emulator/artifacts/
tests/emulator/fixtures/tmp_preflight/

# Kernel-patch routing config (Kai-zen-OS repo only, not synced upstream)
# These lines define M-KERNEL tier routing that is specific to this
# project's Samsung kernel work and should not propagate to the
# general Nex_Alignment orchestrator template.
kernel_routing.yaml
kernel_routing.json
.kernel-patches/
Docs/architecture/kernel_patch_log.md
